import { describe, it } from 'vitest';

describe('habit form', () => { 
  it.todo('shows a validation error when habit name is empty');
  it.todo('creates a new habit and renders it in the list');
  it.todo('edits an existing habit and preserves immutable fields');
  it.todo('deletes a habit only after explicit confirmation');
  it.todo('toggles completion and updates the streak display');
});