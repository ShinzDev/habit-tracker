import { describe, it } from 'vitest';

describe('auth flow', () => { 
  it.todo('submits the signup form and creates a session');
  it.todo('shows an error for duplicate signup email');
  it.todo('submits the login form and stores the active session');
  it.todo('shows an error for invalid login credentials');
});